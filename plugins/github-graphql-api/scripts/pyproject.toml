[project]
name = "scripts"
version = "0.1.0"
description = "Build scripts for GitHub GraphQL API reference docs"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
build-reference = "build_reference:main"
